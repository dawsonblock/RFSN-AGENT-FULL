[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "demo_failrepo"
version = "0.0.1"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["."]
