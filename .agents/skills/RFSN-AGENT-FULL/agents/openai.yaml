interface:
  display_name: "RFSN AGENT FULL"
  short_description: "Repo-specific patterns and workflows for RFSN-AGENT-FULL"
  default_prompt: "Use the RFSN-AGENT-FULL repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true