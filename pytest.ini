[pytest]
testpaths = tests
norecursedirs =
    data
    fixtures
    .git
    .venv
    venv
    build
    dist
    results
    canary_run.log

# Show short tracebacks for cleaner output.
addopts = --tb=short
